# Núcleo científico
numpy>=1.24
pandas>=2.0
scipy>=1.11

# Modelado clásico
statsmodels>=0.14     # ARIMA/ETS, OLS (HAR)

# ML / escalado
scikit-learn>=1.3

# LSTM (opcional; se omite si no está disponible)
tensorflow>=2.16 ; platform_system!="Linux" or python_version>="3.10"

# Volatilidad
arch>=6.2             # GARCH(1,1)

# Gráficos
matplotlib>=3.8

# Configuración
PyYAML>=6.0

# Prophet (opcional para modo "prophet" y para competir en "auto")
prophet>=1.1.5
cmdstanpy>=1.2

# MetaTrader 5 (solo Windows)
MetaTrader5>=5.0 ; platform_system=="Windows"

# Utilidades extra usadas
beautifulsoup4>=4.12  # calendario (scraping sencillo)
openpyxl>=3.1         # exportar a Excel (ruta_reporte)
