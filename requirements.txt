# --- Núcleo de datos / modelado ---
pandas>=1.5
numpy<2.0
scikit-learn>=1.0
scipy>=1.10
statsmodels>=0.14

# --- Visualización ---
matplotlib>=3.5

# --- Prophet (forecasting) ---
prophet==1.1.5
cmdstanpy>=1.1.0    # ayuda a Prophet a gestionar CmdStan; se descargará al primer uso

# --- MT5 / I/O / utilidades ---
MetaTrader5==5.0.44
requests>=2.28
beautifulsoup4>=4.9
PyYAML>=6.0

# --- Excel ---
openpyxl>=3.0
xlsxwriter>=3.0     # opcional: si no lo quieres, quítalo (hay fallback a openpyxl)

# --- Indicadores técnicos (si usas funciones externas de 'ta') ---
ta==0.10.2

# --- (Opcional) LSTM ---
# Si usarás lstm_adapter.py, instala TensorFlow CPU (más portable que GPU en Windows)
tensorflow-cpu==2.15.*
